let score = "444";

// console.log(typeof score);

let numberOutValue = "as";

let BooleanValue = Boolean(numberOutValue);

console.log(BooleanValue);

// The result is false for the following cases:

// The argument is an empty string "".
// The argument is the number 0.  [missConcunsufsions]
// The argument is NaN (Not a Number).
// The argument is null or undefined.
// The argument is false.

// For any other non-empty string or a non-zero number, =>
// the result is true.

let someNumber = 33;

let stringNumber = String(someNumber);
console.log(typeof someNumber, someNumber);
